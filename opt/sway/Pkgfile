# Description: tiling wm like i3wm, but then wayland compatible
# URL:         https://swaywm.org/
# Depends on:  meson wlroots wayland wayland-protocols pcre json-c pango cairo gdk-pixbuf2

name=sway
version=git
release=master
source="https://github.com/SirCmpwn/sway.git"
build()
{
    cd $SRC/$name
    meson build
    ninja -C build
    ninja -C build install
}

post-install()
{
    chmod a+s /usr/local/bin/sway
    sudo setcap cap_sys_tty_config=eip /usr/local/bin/sway
}
